{
  "name": "projectonnodejs",
  "version": "1.0.0",
  "description": "Project on Raw Node JS",
  "main": "index.js",
  "scripts": {
    "start": "set NODE_ENV=staging && nodemon index",
    "production": "NODE_ENV=production nodemon index",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "sudipta jana",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.0.6",
    "node-fetch": "^3.2.6"
  },
  "dependencies": {
    "dotenv": "^16.0.3"
  }
}
